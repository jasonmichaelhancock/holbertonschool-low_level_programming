#include "holberton.h"
#include <stdio.h>

/**
 * set_string - set pointer to char
 * @s: the double pointer
 * @to: the pointer
 *
 * Return: void.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
